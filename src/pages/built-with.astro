---
import Layout from "@layouts/Layout.astro";
import { GET } from "./builtwith.json.ts";

let response = await GET(Astro);
const data = await response.json();
---

<Layout>
  <pre>{JSON.stringify(data, null, 4)}</pre>
</Layout>
